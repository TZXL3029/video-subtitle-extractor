# 2026-07-30

## Label Dictionary Selection

- Project root: `D:\video-subtitle-extractor`.
- Change: batch auto extraction now loads label dictionaries per JSON file instead of merging all terms into one matcher, so `coverage` is calculated against the selected dictionary only.
- CLI behavior: added `--label` to force one dictionary; without it, each video first tries filename-based dictionary selection and falls back to scoring each dictionary separately when no filename match exists.
- JSON output: selected multi-candidate OCR results now include `text_match_label` alongside `text_match_score` and `text_match_coverage`.
- Docs updated: `README.md` and `docs/batch-auto-extraction-guide.md`.
- Verification: `py -3 -m unittest discover -s test -p "test_label_dictionary_selection.py"` passed; `py -3 -m unittest discover -s test -p "test_short_subtitle_roi_rescue.py"` passed; `py -3 -m py_compile backend\tools\label_text_matcher.py backend\tools\auto_subtitle_area.py scripts\batch_auto_extract.py test\test_label_dictionary_selection.py test\test_short_subtitle_roi_rescue.py` passed; `git diff --check` passed with only existing line-ending warnings.
