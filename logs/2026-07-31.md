# 2026-07-31

## OCR subtitle area grouped boxes

- Scope: extended the OCR subtitle area sidecar JSON so it emits `ocr_subtitle_bboxes`, a list of grouped maximum boxes.
- Merge rule: boxes merge when `intersection_area / min_box_area` reaches the configured overlap threshold and neither width nor height differs beyond the configured max size ratio.
- Defaults: `--ocr-area-overlap-threshold 0.5` and `--ocr-area-max-size-ratio 3.0`.
- Files changed: `backend/tools/ocr_subtitle_area.py`, `backend/main.py`, `scripts/batch_auto_extract.py`, `test/test_short_subtitle_roi_rescue.py`, `docs/batch-auto-extraction-guide.md`.
- Verification: `py -m unittest discover -s test -p "test_short_subtitle_roi_rescue.py" -v` passed; `py -m py_compile backend\tools\ocr_subtitle_area.py backend\main.py scripts\batch_auto_extract.py test\test_short_subtitle_roi_rescue.py` passed.
- Risk: grouping is based on OCR text-box geometry only; real-video behavior may need threshold tuning for unusual subtitle layouts.

### Candidate output fix

- Fixed multi-ROI candidate selection so the selected candidate's OCR area JSON is not lost when the candidate work directory is cleaned.
- Candidate OCR area JSON is now written beside the candidate SRT under the candidate root and `finalize_candidate_selection` can regenerate it from the selected extractor's `raw.txt` as a fallback.
- Verification: `py -m unittest discover -s test -p "test_short_subtitle_roi_rescue.py" -v` passed with 12 tests; `py -m py_compile backend\tools\ocr_subtitle_area.py backend\main.py scripts\batch_auto_extract.py test\test_short_subtitle_roi_rescue.py` passed.
